# Working notes for Sterna

The working agreements and the traps live in **[`AGENTS.md`](AGENTS.md)**, which
is the file every agent on this project reads — not just Claude Code. This file
imports it so that Claude Code loads the same text, and it exists only to do
that.

Do not put project instructions here. A rule written in one of two files is a
rule half the agents working on this repository will never see.

@AGENTS.md
