# Rust
target/
**/*.rs.bk

# CMake / Qt
build/
/shell/build
# A second build tree, Release, for the perf gate — the numbers are wrong out
# of a Debug one. See bench/README.md.
/shell/build-release
*.o
*.so
*.a
compile_commands.json

# Oracle spike artifacts
/oracle/build
/oracle/out

# Editors
.vscode/
.idea/
*.swp
